name: proctor
on:
  pull_request:
  push:
    branches: [main]
permissions:
  security-events: write
  contents: read
jobs:
  proctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./
