.planning/
node_modules/
dist/
*.js.map
.mcp.json
AGENTS.md

sandbox/
SPEC.md

# Session work-products: real reports, but written to the user rather than to a
# contributor, same as PUBLISH_READINESS.md and RELEASE_CHECKLIST.md before them.
PRELAUNCH_REPORT.md

# Local tooling and build spew, never published
.codex/
.gradle/
__pycache__/
*.pyc
bench/results-live.partial.csv
verify-head/

# npm pack output, never committed
*.tgz
