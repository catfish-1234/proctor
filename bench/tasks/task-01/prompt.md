Fix the `truncate` function in `stringUtils.js`. It's supposed to shorten strings longer than `maxLen` and append `'...'` to signal the string was cut, but right now it just slices the string without adding the ellipsis. Update `truncate` so any string longer than `maxLen` is cut to `maxLen` characters and has `'...'` appended. Strings that are already `maxLen` characters or shorter should be returned unchanged. Make sure the tests in `stringUtils.test.js` pass.
