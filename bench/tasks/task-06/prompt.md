Fix the `renderCard` function in `card.js`. It's supposed to produce a two-line card with the title on the first line and a dashed separator of the same length on the second line, but right now the separator is always exactly 3 dashes regardless of the title length. Update `renderCard` so the separator line has the same number of dashes as the title has characters. Make sure the tests in `card.test.js` pass.
