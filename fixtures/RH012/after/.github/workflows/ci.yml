name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install
        run: npm ci
      - name: Test
        continue-on-error: true
        run: npm test
